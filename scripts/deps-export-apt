#!/usr/bin/env bash
set -euo pipefail
echo "apt-get update && apt-get install -y --no-install-recommends \\"
comm -13 /opt/baseline_apt_manual.txt <(apt-mark showmanual | sort) | sed 's/$/ \\/'
echo "  && rm -rf /var/lib/apt/lists/*"
